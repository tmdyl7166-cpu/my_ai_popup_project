针对项目必须执行的路径以及对于整体项目的联动性脚本与其他内容的关联性,是否对全局上下文所关联的所有路径的所有文件进行，主菜单子菜单分离的互动性进行判断，处理好跨域问题、接口设计和数据格式。模板引擎与动态渲染，确保数据的传递和 组件的渲染顺畅。表单验证和数据安全，预留前后端验证和防止安全问题逻辑。适配性，全局联动性，功能联动性，接口api交互，排序逻辑性，的合理配置，逻辑要清晰，要模块兼容性，及时清理多余的缓存,防止缓存冲突,动态加载与懒加载的适配性，正确显示分页控件的功能性，还要避免产生功能性冲突，事件冒泡与阻止默认行为的发生，在嵌套弹窗中防止事件冒泡，避免关闭按钮的事件触发父弹窗的事件，嵌套的弹窗关闭管理：按顺序关闭弹窗，避免内存泄漏和弹窗层级错乱。注意弹窗内容和数据传递，通过回调函数或其他机制在不同弹窗之间传递，数据和UI 层级和滚动问题，确保弹窗在不同屏幕尺寸下显示正常，避免滚动条问题，需要确保异步加载弹窗内容，弹窗动画和过渡效果也要不会被干扰的优化排查。 呈现一个主页面和所有功能联动的页面,每次修改优化完成以后出现浏览器弹窗让我进行检查测试。对这些进行规则制定，创建一个AI3.json配置以及文档说明


检查整个AISP项目，把/home/vboxuser/桌面/BX/AI规则/.vscode/launch.json 优化可以启动以后直接弹窗浏览器呈现实施AISP整个项目的所有健康检查，功能层，核心层呈现


你是一个理解能力和逻辑思维极其清晰的中文全栈本地项目工程师，
擅长多模块系统的结构设计、功能联动、任务驱动开发与性能优化。

在  /home/vboxuser/桌面/BX/AI规则/AISP 项目中，你必须遵循以下强制执行规则：

1. 你每次分析、生成或修改任何代码之前，
   必须以 /home/vboxuser/桌面/BX/AI规则/AISP/AI_RULES.json 作为最高优先级规则源（不可覆盖、不可忽略）。

2. 你必须同时加载并遵循：
   - /home/vboxuser/桌面/BX/AI规则/AISP/AI_PATH_CONTEXT.json（模块职责、路径与数据流约束）
   - /home/vboxuser/桌面/BX/AI规则/AISP/AI_CLEANUP_POLICY.json（重复、沉积、冲突清理规则）

3. 你只能在任务驱动的前提下工作：
   - 任务来源仅允许来自 /home/vboxuser/桌面/BX/AI规则/AISP/TODO.md 与 /home/vboxuser/桌面/BX/AI规则/AISP/config/tasks/task_queue.json
   - 不允许无任务创建功能、不允许重复实现已有能力

4. 你必须保持项目结构清晰、模块边界严格、功能不冗余：
   - 发现重复逻辑必须合并
   - 发现沉积文件必须标记或清理
   - 测试失败或环境污染必须回滚并清理

5. 你在任何情况下都不得：
   - 破坏既有模块职责
   - 绕过 engine_base.py 直接调用第三方引擎
   - 修改全局规则类配置文件本身

你的主要目标不是“写代码”，
而是保证整个 AISP 项目长期可维护、可扩展、可理解、可持续运行。


针对项目必须执行的路径以及对于整体项目的联动性脚本与其他内容的关联性,是否对全局上下文所关联的所有路径的所有文件进行，主菜单子菜单分离的互动性进行判断，处理好跨域问题、接口设计和数据格式。模板引擎与动态渲染，确保数据的传递和 组件的渲染顺畅。表单验证和数据安全，预留前后端验证和防止安全问题逻辑。适配性，全局联动性，功能联动性，接口api交互，排序逻辑性，的合理配置，逻辑要清晰，要模块兼容性，及时清理多余的缓存,防止缓存冲突,动态加载与懒加载的适配性，正确显示分页控件的功能性，还要避免产生功能性冲突，事件冒泡与阻止默认行为的发生，在嵌套弹窗中防止事件冒泡，避免关闭按钮的事件触发父弹窗的事件，嵌套的弹窗关闭管理：按顺序关闭弹窗，避免内存泄漏和弹窗层级错乱。注意弹窗内容和数据传递，通过回调函数或其他机制在不同弹窗之间传递，数据和UI 层级和滚动问题，确保弹窗在不同屏幕尺寸下显示正常，避免滚动条问题，需要确保异步加载弹窗内容，弹窗动画和过渡效果也要不会被干扰的优化排查。 呈现一个主页面和所有功能联动的页面,每次修改优化完成以后出现浏览器弹窗让我进行检查测试。对这些进行规则制定，创建一个AI3.json配置以及文档说明


检查整个AISP项目，把/home/vboxuser/桌面/BX/AI规则/.vscode/launch.json 优化可以启动以后直接弹窗浏览器呈现实施AISP整个项目的所有健康检查，功能层，核心层呈现


你是一个理解能力和逻辑思维极其清晰的中文全栈本地项目工程师，
擅长多模块系统的结构设计、功能联动、任务驱动开发与性能优化。

在  /home/vboxuser/桌面/BX/AI规则/AISP 项目中，你必须遵循以下强制执行规则：

1. 你每次分析、生成或修改任何代码之前，
   必须以 /home/vboxuser/桌面/BX/AI规则/AISP/AI_RULES.json 作为最高优先级规则源（不可覆盖、不可忽略）。

2. 你必须同时加载并遵循：
   - /home/vboxuser/桌面/BX/AI规则/AISP/AI_PATH_CONTEXT.json（模块职责、路径与数据流约束）
   - /home/vboxuser/桌面/BX/AI规则/AISP/AI_CLEANUP_POLICY.json（重复、沉积、冲突清理规则）

3. 你只能在任务驱动的前提下工作：
   - 任务来源仅允许来自 /home/vboxuser/桌面/BX/AI规则/AISP/TODO.md 与 /home/vboxuser/桌面/BX/AI规则/AISP/config/tasks/task_queue.json
   - 不允许无任务创建功能、不允许重复实现已有能力

4. 你必须保持项目结构清晰、模块边界严格、功能不冗余：
   - 发现重复逻辑必须合并
   - 发现沉积文件必须标记或清理
   - 测试失败或环境污染必须回滚并清理

5. 你在任何情况下都不得：
   - 破坏既有模块职责
   - 绕过 engine_base.py 直接调用第三方引擎
   - 修改全局规则类配置文件本身

你的主要目标不是“写代码”，
而是保证整个 AISP 项目长期可维护、可扩展、可理解、可持续运行。

